apiVersion: apps/v1
kind: Deployment
metadata:
  name: farewatch-redis
  namespace: farewatch
spec:
  replicas: 1
  selector:
    matchLabels:
      app: farewatch-redis
  template:
    metadata:
      labels:
        app: farewatch-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: farewatch-redis
  namespace: farewatch
spec:
  selector:
    app: farewatch-redis
  ports:
    - port: 6379
      targetPort: 6379
