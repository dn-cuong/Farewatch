apiVersion: batch/v1
kind: CronJob
metadata:
  name: farewatch-scanner
  namespace: farewatch
spec:
  # Scheduled fare scans on EKS (UTC unless the cluster sets another timezone).
  schedule: "*/15 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        metadata:
          labels:
            app: farewatch-scanner
        spec:
          restartPolicy: Never
          containers:
            - name: scanner
              image: farewatch/api:latest
              imagePullPolicy: IfNotPresent
              command: ["/app/scanner"]
              envFrom:
                - configMapRef:
                    name: farewatch-config
                - secretRef:
                    name: farewatch-secrets
              resources:
                requests:
                  cpu: 100m
                  memory: 128Mi
                limits:
                  cpu: "1"
                  memory: 512Mi
